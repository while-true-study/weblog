// In a real Vite app, use import.meta.env.VITE_API_URL
// For this environment, we hardcode based on the user request.
export const API_BASE_URL = 'http://localhost:8080/api/v1';

export const PAGINATION_SIZE = 10;
